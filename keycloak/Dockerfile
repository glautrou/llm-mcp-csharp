# Keycloak avec le realm de démonstration embarqué : rien à monter, rien à
# configurer à la main. Le realm est importé au premier démarrage.
FROM quay.io/keycloak/keycloak:26.7.1

COPY realm-todo-mcp.json /opt/keycloak/data/import/realm-todo-mcp.json
