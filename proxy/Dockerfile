# Configuration embarquée dans l'image plutôt que montée en volume :
# les chemins d'hôte varient d'un système à l'autre, pas le contenu d'une image.
FROM caddy:2-alpine

COPY Caddyfile /etc/caddy/Caddyfile
